rules_version = '2';

// All storage operations go through the API (admin SDK bypasses rules).
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
